# MOVED — see xiom-http/ARCHITECTURE.md

This scratch design document has been migrated into its package repository:

**New location:** `ecosystem/xiom-http/ARCHITECTURE.md`

Citation markers were cleaned up and a "Current State vs Target" section was added
during the move. This file is superseded and safe to delete:

```
git rm ecosystem/XIOM_HTTP_ARCHITECTURE.md
```
