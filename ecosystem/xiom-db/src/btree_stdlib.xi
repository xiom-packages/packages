module xiom.db.legacy.btree_stdlib
// REMOVED — this file is dead and should be deleted.
// It called xiom.collections.BTreeMap.range()/.iter()/.first()/.last(), none of
// which exist in stdlib, so it did not compile. The flat-array implementation in
// src/index/btree.xi (module xiom.db.index.btree) is the single canonical B-tree.
