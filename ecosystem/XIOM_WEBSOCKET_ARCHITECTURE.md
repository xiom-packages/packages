# MOVED — see xiom-websocket/ARCHITECTURE.md

This scratch design document has been migrated into its package repository:

**New location:** `ecosystem/xiom-websocket/ARCHITECTURE.md`

Citation markers were cleaned up and the package scaffold (package.xi, README.md,
SPEC.md, docs/) was created during the move. This file is superseded and safe to delete:

```
git rm ecosystem/XIOM_WEBSOCKET_ARCHITECTURE.md
```
