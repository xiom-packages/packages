# MOVED — see xiom-graphql/ARCHITECTURE.md

This scratch design document has been migrated into its package repository:

**New location:** `ecosystem/xiom-graphql/ARCHITECTURE.md`

Citation markers were cleaned up and the package scaffold (package.xi, README.md,
SPEC.md, docs/, schema/) was created during the move. This file is superseded and safe to delete:

```
git rm ecosystem/XIOM_GRAPHQL_ARCHITECTURE.md
```
