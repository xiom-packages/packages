# xiom.gcp

> **Status:** PLACEHOLDER -- reserved, spec pending. No implementation yet.
> **Scope:** Bindings for Google Cloud Platform services (storage, compute, functions, analytics).
> **Deps:** stdlib; may wrap C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `gcs` | Cloud Storage buckets and objects |
| `gce` | Compute Engine instance management |
| `cloudfunctions` | Serverless function deployment |
| `bigquery` | SQL analytics and data warehouse |
| `pubsub` | Publish/subscribe messaging |
