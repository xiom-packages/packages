# xiom-rest

> **Status:** PLACEHOLDER — reserved, spec pending. No implementation yet.
> **Scope:** RESTful API layer built on the HTTP package.
> **Deps:** stdlib; may wrap C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `rest-server` | REST server with resource and endpoint registration. |
| `rest-client` | Typed client for consuming REST APIs. |
| `resource` | Resource modeling and CRUD routing. |
| `middleware` | Auth, logging, and CORS middleware chain. |
| `json` | JSON content negotiation and body handling. |
