# xiom-spi

> **Status:** PLACEHOLDER -- reserved, spec pending. No implementation yet.
> **Scope:** SPI serial peripheral interface master/slave data transfer.
> **Deps:** stdlib; may wrap C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `master` | SPI master transfer transactions. |
| `slave` | SPI slave receive/transmit. |
| `cs` | Chip-select management. |
| `mode` | Clock polarity and phase configuration. |
