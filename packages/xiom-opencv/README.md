# xiom.opencv

> **Status:** PLACEHOLDER -- reserved, spec pending. No implementation yet.
> **Scope:** OpenCV bindings for computer vision.
> **Deps:** stdlib; may wrap C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `core` | Core structures and arrays. |
| `imgproc` | Image filtering and transforms. |
| `imgcodecs` | Image read/write. |
| `features` | Feature detection and descriptors. |
| `dnn` | Deep neural network module. |
