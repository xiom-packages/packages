# xiom-graphql

> **Status:** PLACEHOLDER -- reserved, spec pending. No implementation yet.
> **Scope:** GraphQL server/client with schema and query execution.
> **Deps:** stdlib; may wrap C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `graphql-server` | GraphQL server executing queries over HTTP. |
| `graphql-client` | GraphQL client for query and mutation requests. |
| `schema` | Type, field, and directive schema definition. |
| `parser` | GraphQL document lexer and parser. |
| `resolver` | Resolver tree and field execution engine. |
| `validation` | Query validation against the schema. |
