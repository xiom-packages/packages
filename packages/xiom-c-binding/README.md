# xiom.c-binding

> **Status:** PLACEHOLDER -- reserved, spec pending. No implementation yet.
> **Scope:** Thin FFI bindings to common C libraries umbrella.
> **Deps:** stdlib; wraps C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `libcurl` | cURL HTTP client bindings |
| `libgit2` | git2 library bindings |
| `libssh2` | SSH2 library bindings |
| `libxml2` | XML2 library bindings |
| `libexpat` | Expat XML parser bindings |
| `libyaml` | YAML library bindings |
| `libjansson` | Jansson JSON bindings |
