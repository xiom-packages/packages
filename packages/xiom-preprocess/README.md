# xiom.preprocess

> **Status:** PLACEHOLDER -- reserved, spec pending. No implementation yet.
> **Scope:** Data preprocessing and cleaning utilities.
> **Deps:** stdlib; may wrap C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `scaler` | Min-max and z-score scaling. |
| `normalize` | Vector normalization routines. |
| `impute` | Missing value imputation. |
| `onehot` | One-hot encoding. |
| `standardize` | Mean/variance standardization. |
| `pipeline` | Composable preprocessing pipelines. |
