# xiom-analyzer

> **Status:** PLACEHOLDER — reserved, spec pending. No implementation yet.
> **Scope:** Semantic analysis, name resolution, and type checking.
> **Deps:** stdlib; may wrap C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `types` | Type representation and unification |
| `resolve` | Name resolution and symbol table |
| `check` | Type checking and constraint solving |
| `infer` | Type inference engine |
| `borrow` | Ownership and borrow-check analysis |
| `errors` | Semantic error reporting and recovery |
