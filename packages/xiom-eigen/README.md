# xiom.eigen

> **Status:** PLACEHOLDER -- reserved, spec pending. No implementation yet.
> **Scope:** Eigen-style dense and sparse linear algebra.
> **Deps:** stdlib; may wrap C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `matrix` | Dense matrix and vector types. |
| `vector` | Vector arithmetic and norms. |
| `solver` | Linear system solvers. |
| `eigen` | Eigendecomposition. |
| `sparse` | Sparse matrix operations. |
