# xiom-wireless

> **Status:** PLACEHOLDER — reserved, spec pending. No implementation yet.
> **Scope:** Wireless networking management and driver bindings.
> **Deps:** stdlib; may wrap C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `iw` | Wireless config (iw) bindings |
| `wpa` | WPA supplicant bindings |
| `hostapd` | Access point daemon bindings |
| `driver` | Wireless driver bindings |
