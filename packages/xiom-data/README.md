# xiom-data

> **Status:** PLACEHOLDER — reserved, spec pending. No implementation yet.
> **Scope:** Data loading, batching and dataset utilities.
> **Deps:** stdlib; may wrap C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `dataset` | Dataset container and iterators. |
| `loader` | Batched data loader. |
| `sampler` | Sample ordering and weighting. |
| `batch` | Batch assembly and collation. |
| `shuffle` | Deterministic shuffling. |
| `split` | Train/validation/test splitting. |
