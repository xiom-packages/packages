# xiom-cassandra

> **Status:** PLACEHOLDER -- reserved, spec pending. No implementation yet.
> **Scope:** Apache Cassandra client: CQL queries, prepared statements, and schema metadata.
> **Deps:** stdlib; may wrap C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `cassandra` | Cassandra native-protocol client (connect, CQL execution). |
| `cassandra_stmt` | Prepared statements with typed bindings. |
| `cassandra_meta` | Cluster, keyspace, and table metadata access. |
