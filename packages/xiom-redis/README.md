# xiom.redis

> **Status:** PLACEHOLDER -- reserved, spec pending. No implementation yet.
> **Scope:** Redis client: RESP protocol, commands, and pub/sub.
> **Deps:** stdlib; may wrap C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `redis` | Redis RESP protocol client (connect, commands, pipelines). |
| `redis_pubsub` | Publish/subscribe channels and message handling. |
