# xiom-ldap

> **Status:** PLACEHOLDER -- reserved, spec pending. No implementation yet.
> **Scope:** LDAP directory bind, search, and user provisioning.
> **Deps:** stdlib; may wrap C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `client` | LDAP connection and bind |
| `search` | Directory search and filters |
| `users` | User/group provisioning |
| `auth` | LDAP-based authentication backend |
