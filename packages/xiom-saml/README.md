# xiom-saml

> **Status:** PLACEHOLDER -- reserved, spec pending. No implementation yet.
> **Scope:** SAML assertions, SSO flows, and metadata handling.
> **Deps:** stdlib; may wrap C (FFI).

## Libs inventory

| Lib | Description |
|-----|-------------|
| `assertion` | SAML assertion parsing and validation |
| `sso` | SP-initiated SSO flow |
| `metadata` | Identity provider metadata handling |
| `sign` | XML signature and verification |
